# Mini-Project
# Simple Task Manager (CLI)

A command-line task manager built in Python.
Tasks are stored in a JSON file (no database).

## Features
- Add new task
- View all tasks
- Mark task as done
- Save and load from tasks.json
- Error handling included

## How to Run

```bash
python tasks.py
